# Public (required for Supabase Auth on the client)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only (optional; do not expose to the browser)
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=

# Local CLI auth (optional; keep in `.env.local`, not in git)
VERCEL_TOKEN=
SUPABASE_ACCESS_TOKEN=

